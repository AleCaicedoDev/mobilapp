// ignore_for_file: file_names

import 'package:flutter/material.dart';

class CategoriaPages extends StatelessWidget {
  const CategoriaPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('CategoriaPages'),
    );
  }
}
