import 'package:flutter/material.dart';

class CarritoPages extends StatelessWidget {
  const CarritoPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('PedidoPages'),
    );
  }
}
