import 'package:flutter/material.dart';

class PerfilPages extends StatelessWidget {
  const PerfilPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('PerfilPages'),
    );
  }
}
